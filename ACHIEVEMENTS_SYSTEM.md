# 🏆 Система достижений и прогресса

## 📝 Описание

Система достижений позволяет игрокам получать уведомления при достижении определённых целей в играх. Включает:

- ✅ Автоматическое отслеживание прогресса игр
- 🎯 12 различных достижений
- 🔔 Красивые уведомления при получении
- 💾 Сохранение в Firebase и localStorage
- 🔄 Синхронизация в реальном времени

## 🎮 Доступные достижения

### Основные достижения:
- **`first_game`** - Сыграй в первую игру
- **`five_levels`** - Пройди 5 уровней
- **`ten_levels`** - Пройди 10 уровней
- **`hundred_points`** - Набери 100 баллов
- **`five_hundred_points`** - Набери 500 баллов
- **`persistent_player`** - Сыграй 10 раз

### Игровые достижения:
- **`find_me_master`** - Мастер поиска (пройди все уровни "Найди меня")
- **`puzzle_master`** - Мастер пазлов (пройди все уровни "Пазл")
- **`truth_seeker`** - Искатель правды (пройди все уровни "Правда или миф")
- **`animal_expert`** - Знаток животных (пройди все уровни "Кто что ест?" и "Кто где живёт?")
- **`completionist`** - Всё пройдено! (пройди все игры)

### Специальные достижения:
- **`speed_demon`** - Скоростной демон (набери 50 баллов в одной игре)

## 🔧 Техническая реализация

### Файлы системы:

1. **`js/profile.js`** - Основная логика достижений
2. **`css/profile.css`** - Стили уведомлений
3. **`js/games/game-manager.js`** - Проверка достижений после игр
4. **`js/debug-achievements.js`** - Функции для тестирования
5. **`firebase-database-structure.json`** - Обновлённая структура БД

### Основные функции:

#### `checkForNewAchievements(stats, userId)`
Проверяет статистику и показывает уведомления о новых достижениях

#### `showAchievementNotification(achievement)`
Показывает красивое уведомление с анимацией

#### `saveUnlockedAchievements(achievementIds, userId)`
Сохраняет разблокированные достижения в Firebase/localStorage

## 🧪 Тестирование

### Автоматическое тестирование:
Система работает автоматически при завершении уровня игры:

1. Игрок завершает уровень → Сохраняется прогресс → Проверяются достижения → Показывается уведомление

### Ручное тестирование через консоль:
Откройте консоль браузера на странице профиля и используйте команды:

```javascript
// Показать текущую статистику
window.debugAchievements.showCurrentStats();

// Симулировать игру (разблокирует "Первая игра")
window.debugAchievements.simulateGameProgress('findMe', 1, 20);

// Разблокировать конкретное достижение
window.debugAchievements.unlockSpecificAchievements(['first_game']);

// Сбросить все достижения
window.debugAchievements.resetAchievements();
```

### Тест-кейсы:

#### ✅ Тест 1: Получение первого достижения
```javascript
// Выполнить в консоли:
window.debugAchievements.simulateGameProgress('findMe', 1, 10);
```
**Ожидаемый результат**: Уведомление "Первая игра"

#### ✅ Тест 2: Набор баллов
```javascript
// Набрать 100 баллов:
window.debugAchievements.simulateGameProgress('findMe', 1, 50);
window.debugAchievements.simulateGameProgress('findMe', 2, 50);
```
**Ожидаемый результат**: Уведомление "Сто баллов"

#### ✅ Тест 3: Прохождение уровней
```javascript
// Пройти 5 уровней:
for(let i = 1; i <= 5; i++) {
    window.debugAchievements.simulateGameProgress('findMe', 1, 10);
}
```
**Ожидаемый результат**: Уведомления "Пять уровней"

## 🎨 Визуальные особенности

### Уведомления:
- Анимированный вход справа
- Иконка достижения с эффектом bounce
- Звуковой сигнал
- Автоматическое скрытие через 5 секунд
- Кнопка закрытия
- Адаптивный дизайн для мобильных

### Карточки достижений:
- Заблокированные: серые с иконкой замка
- Разблокированные: цветные с hover-эффектами
- Анимация пульсации при наведении

## 📊 Структура данных

### В Firebase:
```
users/
  {userId}/
    unlockedAchievements: ["first_game", "five_levels", ...]
    progress: {
      games: { ... },
      totalScore: 100,
      levelsCompleted: 5,
      gamesPlayed: 3
    }
```

### Локальное хранение:
```javascript
// localStorage
achievements_{userId}: ["first_game", "five_levels"]
gameProgress_{userId}: { ... }
```

## 🔄 Real-time синхронизация

- Достижения синхронизируются между устройствами через Firebase
- Уведомления показываются только при новых достижениях
- При входе в систему загружается полная статистика

## 🚀 Интеграция с играми

### GameManager автоматически:
1. Сохраняет прогресс после завершения уровня
2. Проверяет достижения
3. Показывает уведомления

### Поддерживаемые игры:
- Найди меня (findMe)
- Кто что ест (whoEats)
- Пазл (puzzle)
- Кто где живёт (whoLives)
- Правда или миф (truthMyth)

## 🎯 Возможности расширения

### Добавление нового достижения:
```javascript
// В js/profile.js, массив achievements:
{
    id: 'new_achievement',
    name: 'Новое достижение',
    description: 'Описание достижения',
    icon: '🎯',
    condition: (stats) => stats.totalScore >= 1000
}
```

### Дополнительные уведомления:
```javascript
// Добавить в showAchievementNotification()
notification.classList.add('custom-achievement');
```

## 🔧 Отладка

### Проверка состояния:
```javascript
// В консоли:
console.log('Разблокированные достижения:', unlockedAchievements);
console.log('Текущая статистика:', await getAllProgressStats());
```

### Логи в консоли:
- `✅ Достижения сохранены в Firebase`
- `🏆 Достижение получено!`
- `📊 Обновление статистики профиля в реальном времени`

## 📱 Мобильная поддержка

- Уведомления адаптированы под мобильные экраны
- Анимации оптимизированы для touch-устройств
- Hover-эффекты заменены на touch-события